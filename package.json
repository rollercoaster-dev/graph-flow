{
  "name": "graph-flow",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rollercoaster-dev/graph-flow.git"
  },
  "author": "rollercoaster.dev",
  "engines": {
    "bun": ">=1.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build --filter=./packages/*",
    "test": "bun test",
    "lint": "biome check packages/",
    "lint:fix": "biome check --write packages/",
    "migrate": "bun run scripts/migrate-to-v2.ts",
    "mcp": "bun run packages/mcp/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.0.0"
  },
  "description": "File-based workflow checkpoint, knowledge management, and code graph analysis for Claude Code"
}
