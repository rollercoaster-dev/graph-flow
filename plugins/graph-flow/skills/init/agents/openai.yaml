interface:
  display_name: "graph-flow init"
  short_description: "Bootstrap graph-flow for Codex in the current repository."
  default_prompt: "Use graph-flow init to configure this repository for Codex and verify doctor passes."
policy:
  allow_implicit_invocation: false
