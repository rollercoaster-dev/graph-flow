interface:
  display_name: "graph-flow auto-issue"
  short_description: "Run one issue end-to-end without manual approval gates."
  default_prompt: "Use graph-flow auto-issue for issue #31 and take it from setup through PR creation."
policy:
  allow_implicit_invocation: false
